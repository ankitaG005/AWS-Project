#!/bin/bash

# AWS CLI command to create an S3 bucket
BUCKET_NAME=myclibucket005
aws s3api create-bucket --bucket $BUCKET_NAME --region us-east-1 --acl private   


































































